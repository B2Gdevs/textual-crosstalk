---
id: h-2026-05-15T17-57-55-conduit-proj-01
projectid: conduit-proj
phase: 01
task_id: CONDUIT-PROJ-T-01-01
created_at: 2026-05-15T17:57:55.766Z
created_by: unknown
claimed_by: null
claimed_at: null
completed_at: null
priority: normal
estimated_context: prescribed
risk: safe
time: standard
surface: local
runtime_preference: claude-code
---
HANDOFF — conduit_proj Part 1 (2026-05-15 ~13:00 local)

You are Claude Code in C:/Users/benja/Documents/conduit_proj/. This hydrates you so you focus on shipping.

## Done (don't redo)
1. .planning/ populated. Read: gad snapshot --projectid conduit_proj
2. Entrypoints: AGENTS.md (source), CLAUDE.md (pointer), GEMINI.md, .cursorrules, .opencode/AGENTS.md, SOUL.md.
3. Env: .gitignore (protects .env*/.gad/secrets/JSONL/.venv), .env.example (full keys), .env.local (Deepgram populated; Groq + ElevenLabs empty — paste yours).
4. Reqs 8 + decisions 11 + phases 4 + tasks 7.

## Stack (decided D-001..D-010)

| Layer | Pick |
|---|---|
| Lang | Python 3.11+ |
| UI | Textual TUI |
| UI patterns | ~/Documents/slm_learning/scripts/learning_tui/ — chat_screen.py 519 LOC + local_speech.py 168 LOC. Cite module names in comments; DO NOT copy verbatim |
| Audio | sounddevice |
| STT | Deepgram WS, interim_results=True, punctuate=True |
| LLM | Groq llama-3.1-8b-instant (fallback Cerebras) |
| TTS | ElevenLabs text_to_speech.with_timestamps (native char-level timing) |
| Char store | JSONL, dataclass CharEntry(char,start_time,end_time,notes) |
| STT char interp | char_start = word_start + (i/n) * word_duration |
| Repo | Public GitHub, add RioPopper collaborator OR share URL |

## Build order

1. Scaffold (T-01-01): requirements.txt, scripts/conduit_tui/ skeleton, main.py.
2. Char-timeline (T-01-05): append-only JSONL. notes='user,interpolated' / 'bot,native' / 'bot,interpolated'.
3. STT (T-01-02): mic_capture.py + deepgram_client.py. Target: word in store <300ms after utterance end.
4. LLM+TTS (T-01-03+04): groq_client.py + elevenlabs_client.py.
5. Orchestrator (T-01-06): asyncio loop.
6. UI (T-01-07): conversation_screen.py — RichLog user, RichLog bot, DataTable last-N chars, status bar.

## Constraints

- Deepgram key NEVER on GitHub. .gitignore excludes .env.local; verify before push.
- 'hello world' = 11 entries (10 + 1 space) per R-003.
- TTS: ElevenLabs native timestamps, notes='bot,native'.
- STT: linear interp, notes='user,interpolated'.
- Single JSONL, both types, notes distinguishes.
- README documents env vars + run + data format.

## Part 2 (only after Part 1 manual-test passes)

Study tarzain/crosstalk. Capture as D-011. Implement per R-008 + phase 02.

## Ship

- New public repo under B2Gdevs
- Add RioPopper collaborator OR share URL
- gad state log 'shipped'
- gad tasks stamp each CONDUIT-PROJ-T-01-* done

## Acceptance gate

- All 11 chars of 'hello world' appear in the unified JSONL with correct char-level timestamps after a manual test utterance
- LLM response is audible via ElevenLabs voice and its chars appear in the same JSONL with notes='bot,native'
- End-to-end loop runs continuously: user speaks → bot responds → user speaks again, all turns appended
- Word-to-saved STT latency <300ms (measured from Deepgram is_final callback)
- .env.local is NOT in git status before commit; .gitignore verified
- README documents env vars + run command + data format
- GitHub repo created and shared with RioPopper

## Start

  cd ~/Documents/conduit_proj
  gad snapshot --projectid conduit_proj
  gad handoffs list --mine-first --projectid conduit_proj
  gad handoffs claim <this-handoff-id>